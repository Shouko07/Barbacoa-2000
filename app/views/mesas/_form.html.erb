<%= form_with(model: mesa) do |form| %>
  <% if mesa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mesa.errors.count, "error") %> prohibited this mesa from being saved:</h2>

      <ul>
        <% mesa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :numero, style: "display: block" %>
    <%= form.number_field :numero %>
  </div>

  <div>
    <%= form.label :disponibilidad, style: "display: block" %>
    <%= form.text_field :disponibilidad %>
  </div>

  <div>
    <%= form.label :capacidad, style: "display: block" %>
    <%= form.number_field :capacidad %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
