<% content_for :title, "Selecciona una mesa" %>
<!-- filepath: c:\Users\USER\Barbacoa-2000\app\views\mesas\index.html.erb -->
    <% if empleado_signed_in? %>

<div style="text-align: center; margin-bottom: 1.5rem;">
  <%= link_to "Crear nueva mesa", new_mesa_path, class: "btn btn-primary", style: "background-color: #603719; color: white; border-radius: 5px; padding: 8px 18px; text-decoration: none; font-weight: 600;" %>
</div>
         <% end %>

<div class="cuadro-wrapper">
            <table class="table table-striped table-hover">
            <tbody>
                <% @mesas.each do |mesa| %>
                    <%= render mesa %>
                <% end %>
                </tbody>
            </table>
</div>