
<% content_for :title, "Inventario" %>
<script>
$(document).ready(function(){
	$('[data-toggle="tooltip"]').tooltip();
});
</script>
<div class="container">
    <div class="table-responsive">
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-xs-5">
                        <h2><b>Inventario</b></h2>
                    </div>
                    <div class="col-xs-7">
                    <%= link_to new_inventario_path, class: "btn btn-primary" do %>
                        <i class="material-icons">&#xE147;</i>
                        <span>Agregar a inventario</span>
                      <% end %>
                      <%= link_to empleados_path, class: "btn btn-primary" do %> 
                        <i class="material-icons">&#xE24D;</i>
                        <span>Exportar a Excel</span>
                      <% end %>
                    </div>
                </div>
            </div>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th class="text-center">Producto</th>
                        <th class="text-center">Cantidad</th>
                        <th class="text-center">Tipo de almacenamiento</th>
                        <th class="text-center">Accion</th>
                    </tr>
                </thead>
            <tbody>
                <% @inventarios.each do |inventario| %>
                    <%= render inventario %>
                <% end %>
                </tbody>
            </table>
            <div class="clearfix">
                <div class="hint-text">Showing <b>5</b> out of <b>25</b> entries</div>
                <ul class="pagination">
                    <li class="page-item disabled"><a href="#">Previous</a></li>
                    <li class="page-item"><a href="#" class="page-link">1</a></li>
                    <li class="page-item"><a href="#" class="page-link">2</a></li>
                    <li class="page-item active"><a href="#" class="page-link">3</a></li>
                    <li class="page-item"><a href="#" class="page-link">4</a></li>
                    <li class="page-item"><a href="#" class="page-link">5</a></li>
                    <li class="page-item"><a href="#" class="page-link">Next</a></li>
                </ul>
            </div>
        </div>
    </div>        
</div>